/*
**  Usage:
**      determine_action(str)
**
**  Arguments:
**      str         a set of characters, string
**
**  Returns:
**      a list with (1) the name of the action that 
**      should occur & (2) the arguments for that action
**
**  Notes:
**      To avoid more string processing (1) above will be
**      an integer value that maps to the name of the action.
**
**  Stewart Cartmell
*/
{
    var str, len
    str = argument0
    len = string_length(str)
    if (len > 0) {
        // do stuff
    } else {
        // return an error?
    }
}
